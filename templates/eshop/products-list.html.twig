{% extends 'default/default.html.twig' %}

{% block content %}
{% if(category) %}
    <div class="page-header">
        <h1>{{ category.name }}</h1>
    </div>
{% endif %}

<h2>Filters</h2>
{#

<form id="js_filterfield">

    {% if(filterDefinition.filters|length > 0) %}

        {% for filter in filterDefinition.filters %}

            {% set filterMarkup = filterService.filterFrontend(filter, listing.products, currentFilter) %}

            {{ filterMarkup | raw  }}

        {% endfor %}
    {% endif %}

</form>

#}
    <div>{{ dump() }}</div>
<div>
    {% if listing.products is not empty %}
    <ul>
        {% for product in listing.products %}
        <li>{{ product.name }}</li>
        {% endfor %}
    </ul>
    {% endif %}
</div>
<input type="text" id="input-field">

{% do pimcore_inline_script().appendScript("
    $('#input-field').on('change', function() {
                alert('It works!');
    });
") %}
{% endblock %}
